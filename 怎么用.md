# 不怕 Codex 罢工：跨电脑安装包

这是一个给 Mac 用的 Codex 本地备份工具。

## 最简单用法

把整个 `codex-backup-portable-kit` 文件夹复制到另一台 Mac。

然后双击：

```text
install.command
```

安装完成后，那台电脑会每天晚上 23:50 自动备份 Codex。

备份会保存到：

```text
~/Documents/不怕codex罢工
```

默认只保留最新 1 份备份：新备份成功以后，旧备份会自动删除。如果新备份失败，旧备份会继续保留。

## 想马上备份

双击：

```text
~/Documents/不怕codex罢工/backup-now.command
```

## 想关闭自动备份

双击：

```text
~/Documents/不怕codex罢工/uninstall.command
```

关闭自动备份不会删除已有备份。

## 备份里有什么

- `~/.codex` 里的 memory、sessions、skills、配置、日志
- `~/Documents/Codex` 里的工作区和输出文件
- `~/.agents/skills`
- Codex App 的本地应用数据

默认不会备份 `auth.json` 登录令牌，这样更安全。

## 注意

这个备份里会有你的聊天记录、memory、项目文件和工作资料。不要发给别人，不要上传公开网盘。
