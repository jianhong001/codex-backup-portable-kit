# 不怕 Codex 罢工：三步使用

1. Mac 双击 `安装-macOS.command`；Windows 双击 `安装-Windows.cmd`。
2. 以后电脑会在每天 23:50 自动备份，不启动 Codex，消耗 0 token。
3. 备份保存在“文档/不怕codex罢工”，默认只保留最新一份。

想立即备份：双击对应的“立即备份”文件。

想关闭定时任务：双击对应的“卸载”文件。已有备份不会删除。

## 换到另一台 Mac

1. 旧 Mac 完全退出 Codex，插入 U 盘，双击 `第1步-旧Mac制作迁移包.command`。
2. 新 Mac 先下载最新版并双击 `安装-macOS.command`，再登录 Codex、至少打开一次，然后完全退出。
3. 把 U 盘接到新 Mac，但不要运行 U 盘里的文件。
4. 在新 Mac 的“文稿/不怕codex罢工”双击本机的 `第2步-新Mac恢复聊天.command`，在弹出的选择框中选 U 盘里的 ZIP。
5. 显示“恢复成功”后重新打开 Codex。

恢复会合并新旧聊天，遇到同 ID 但内容不同的聊天会保留两个可见副本。memory、skills 和项目也会合并，不会复制旧账号的 `auth.json`、Cookie 或登录状态。

旧 Mac 的项目分组也会保留：原项目会显示为“原项目名（旧 Mac 电脑名）”。新 Mac 的同名项目不会被覆盖，会保持两个独立项目。旧 Mac 没有项目的聊天会统一放到“旧 Mac 导入聊天（旧 Mac 电脑名）”。电脑名由旧 Mac 自动写进迁移包。

U 盘迁移包现在只有 ZIP、SHA-256 和说明文字，不再带可执行脚本。首次安装包可能仍需 macOS 确认一次；安装完成后，迁移不再运行 U 盘脚本，也不再弹第二次“开始合并”确认。

只有新 ZIP 完整性检查成功后，旧 ZIP 才会删除。任何失败都不会删除源数据或上一份成功备份。

备份可能包含聊天、memory、代码和工作资料，请勿上传公开网盘或公开 GitHub。
