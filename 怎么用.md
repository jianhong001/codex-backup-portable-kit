# 不怕 Codex 罢工：最简单用法

## 只带走一个项目或一条聊天（Mac → Mac）

不用先安装每日备份。两台 Mac 都把完整工具包解压到本机，然后用同一个入口：`转移选定聊天-macOS.command`。

1. 旧 Mac：保存任务并退出 Codex/ChatGPT，双击入口，选择“导出”，选项目或聊天。
2. 把生成的**一个 ZIP**复制到 U 盘，再带到新 Mac。默认在“文稿/不怕codex罢工/单项迁移”，完成后会自动定位它。
3. 新 Mac：先登录并打开 Codex 一次，再退出；双击同一入口，选择“导入”，选中 ZIP。
4. 完成后重新打开 Codex，在带旧电脑名称的项目下继续聊天、修改文件。

被选中的已归档聊天在新 Mac 上会成为可见的导入副本，旧 Mac 的归档状态不变。

只导入自己的 ZIP，不要解压后运行里面的项目脚本。文件校验能检查损坏，不能验证发件人身份。两台 Mac 的 Codex 数据格式须兼容；不兼容会停止，不强行改数据库。

**会带走：**选中聊天的历史、所属项目的实际文件夹，包括项目内 `AGENTS.md` 等说明。项目可以不在 `Documents/Codex`。

**不会顺带带走：**其他聊天、全局 memory/skills、账号、全局配置、Git 历史、依赖、环境变量和密钥。项目外附件不自动抓取；内嵌图片保留。新版特殊附件索引尚不支持时会拒绝导出。

同一 ZIP 重复导入不会重复新增聊天，也不会覆盖新 Mac 后来改过的文件。重新导出后得到的新包按独立版本导入，保留两边内容。同一选择只保留最新成功导出的 ZIP；不同选择不会相互删除。导入后原 ZIP 留给你保管，不再强制做一次全量备份。

如果 macOS 阻止首次运行，请先核对工具来源，再通过系统提供的“仍要打开”流程授权；不要关闭系统安全保护。

## 每天自动备份

1. Mac 双击 `安装-macOS.command`；Windows 双击 `安装-Windows.cmd`。
2. 以后每天 23:50 自动备份，不启动 Codex，消耗 0 token。
3. 到“文稿/不怕codex罢工”或“文档/不怕codex罢工”查看最新 ZIP。

默认只有一份已验证成功的 ZIP。新备份失败时，旧 ZIP 和电脑原文件都不删除。

想马上备份，双击对应的“立即备份”文件。想关掉每天自动备份，双击对应的“卸载”文件；已有备份仍保留。

## 整台旧 Mac 迁移（旧的高级入口）

仅当确实要迁移整台电脑资料时使用；只带一个项目请用上面的单项入口。旧入口保留签名和配对流程，且依赖 Codex 内部数据格式，不是官方账号迁移。

### 旧 Mac

1. 在“文稿/不怕codex罢工”双击 `第1步-旧Mac制作迁移包.command`。
2. 程序完成后，到“文稿/不怕codex罢工/迁移包”。
3. 只复制 ZIP、同名 `.sha256`、同名 `.signature` 到 U 盘或移动硬盘。
4. 记住程序显示的配对码。

### 新 Mac

1. 安装最新版，登录目标账号，打开 Codex 一次后完全退出。
2. 把三个文件复制到“文稿/不怕codex罢工/待恢复”。里面只能有一个 ZIP。
3. 在新 Mac 本机双击 `第2步-新Mac恢复聊天.command`。
4. 第一次导入这台旧 Mac 时输入配对码。

恢复先校验，再合并。恢复后的新本机备份校验成功后，`待恢复` 的三个文件才会删除。

旧 Mac 的项目会单独保留，聊天合并到左侧任务栏时不会覆盖新 Mac 的已有聊天。`auth.json`、Cookie、旧登录状态和订阅不会迁移。

外接硬盘只放数据，不运行里面的脚本。备份含私人资料，不要上传公开 GitHub 或公开网盘。
